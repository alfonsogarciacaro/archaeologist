
# New Mock Project Documentation

This project is a simple service that reads a configuration file and exposes it.

The service is implemented in `service.js` and the database schema is in `database.sql`.

## Usage

To use the service, call the `get_config` function.
